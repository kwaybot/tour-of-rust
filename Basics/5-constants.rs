// specify common values efficiently
// directly replace text identifiers with value at compile time
// unlike variables, constants must have explicit types
// always written in SCREAMING_SNAKE_CASE

const PI: f32 = 3.14159;

fn main() {
    println!("one pi is {}",PI);
}